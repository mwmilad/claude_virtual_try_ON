# Pinned per upstream (yisol/IDM-VTON) environment.yaml, translated from conda to pip.
# torch 2.0.1 + cu118 already supports Ada Lovelace (RTX 4090, sm_89) -- no bump needed,
# unlike omnitry/ (which needed a newer cu126 wheel for other reasons).
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2

accelerate==0.25.0
transformers==4.36.2
diffusers==0.25.0
torchmetrics==1.2.1
tqdm==4.66.1
einops==0.7.0
bitsandbytes==0.39.0
scipy==1.11.1
opencv-python
huggingface_hub==0.26.5

# densepose (vendored under third_party/IDM-VTON/gradio_demo/densepose) runtime deps.
# detectron2 itself (also vendored, at .../gradio_demo/detectron2) is NOT installed from
# here or anywhere -- it ships as a prebuilt _C.cpython-39-*.so, imported straight off
# sys.path (install.sh just checks that it imports; requires this venv's Python to be
# exactly 3.9, see README.md).
fvcore
cloudpickle
omegaconf
pycocotools
basicsr
av

# human parsing (preprocess/humanparsing) runs its ONNX models on CPU explicitly
# (providers=['CPUExecutionProvider'] in upstream's Parsing class) -- no *-gpu variant needed.
onnxruntime==1.16.2
