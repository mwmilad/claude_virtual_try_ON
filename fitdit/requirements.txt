# Pinned per upstream (BoyuanJiang/FitDiT) README/requirements.txt. Standard PyPI wheels
# for these torch/torchvision versions already ship CUDA runtime support for Ada
# Lovelace (RTX 4090, sm_89) -- no custom --index-url needed here (unlike omnitry/,
# which pins a newer torch that benefits from an explicit cu126 wheel).
torch==2.4.0
torchvision==0.19.0
accelerate==0.31.0
diffusers==0.31.0
transformers==4.39.3
gradio==5.8.0
numpy==1.23.0
scikit-image==0.24.0
huggingface_hub==0.26.5
onnxruntime-gpu==1.20.1
opencv-python
matplotlib==3.8.3
einops==0.7.0
